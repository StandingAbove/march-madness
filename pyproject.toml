[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "march-madness"
version = "0.1.0"
description = "NCAA bracket simulation and Monte Carlo modeling"
requires-python = ">=3.10"
